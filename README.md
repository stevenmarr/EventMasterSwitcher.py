# EventMasterSwitcher.py
An updated python module for communicating with E2/S3 Switchers. Mostly uses the JSONRPC api, but also updates XML nodes directly for commands where no JSONRPC API is available.
